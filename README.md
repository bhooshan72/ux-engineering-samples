# ux-engineering-samples
UX engineering work samples — React + TypeScript + SCSS components with accessible loading/error/empty states, built with an AI-assisted workflow (Claude Code) and reviewed line by line.
